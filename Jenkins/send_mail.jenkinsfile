pipeline {
    agent any
    environment {
        PATH = "/opt/homebrew/bin:${env.PATH}"
    }
    stages {
        stage('Receive Artifacts') {
            steps {
                sh(script: """
                    cd Testsuites
                    ls -lrt
                """)
            }
        }
    }
}
